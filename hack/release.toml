# commit to be tagged for the new release
commit = "HEAD"

project_name = "importvet"
github_repo = "siderolabs/importvet"
match_deps = "^github.com/(talos-systems/[a-zA-Z0-9-]+)$"

# previous = -
# pre_release = true

# [notes]
